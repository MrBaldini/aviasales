import classes from './show-more-btn.module.scss';

const ShowMoreBtn = () => {
  return <button className={classes['content__show-more']}>ПОКАЗАТЬ ЕЩЕ 5 БИЛЕТОВ!</button>;
};

export default ShowMoreBtn;
